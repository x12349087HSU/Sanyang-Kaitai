# 開發紀錄

這份文件記錄「均線篩選器」（Sanyang-Kaitai）專案從 0 到目前狀態的關鍵決策、
考量過的取捨、以及已知限制。目的是讓之後不管是你自己還是協助開發的人（包含
未來的 Claude 對話），不用重新想一次已經想過的問題。

一般的「怎麼安裝、怎麼用」請看 [README.md](README.md)；這份文件是給
「要改程式碼或接續開發的人」看的。

---

## 1. 這個專案跟「公司基本面分析」的關係

「均線篩選器」最初是在「[公司基本面分析](../公司基本面分析/)」（台股投資分析
PDF 報告產生器）專案裡，以「0050 成分股均線篩選」功能的形式開發出來的。後來
使用者要求把這個功能**完全獨立拆分出來**，成為一個自己的資料夾、自己的
git repo（`https://github.com/x12349087HSU/Sanyang-Kaitai`）、自己的 `.venv`，
兩邊互不依賴、各自可以單獨開發部署。

拆分時的做法：不是共用匯入，而是把需要的最小子集（config/cache/http_client/
finmind_client/providers/price、models 精簡版、moving_average）**複製一份**
到這個專案裡自己維護。好處是這個專案完全不需要 pandas/matplotlib——原本公司
基本面分析專案的依賴，這裡都用不到，只需要 `requests` + `streamlit` +
`reportlab`，是刻意的精簡，不是漏掉。

**已知的權衡**：兩邊的股價 provider 邏輯（FinMind + 證交所備援、資料品質過濾）
是分開維護的兩份複本。如果之後在其中一邊修到 bug（例如未來又發現 FinMind
哪個資料集有累計數/單季數搞混之類的問題），**要記得檢查另一邊是否也要同步修**，
不會自動同步。

「公司基本面分析」專案裡原本也留著一份較舊、較簡化版本的 0050 均線篩選（只有
多頭四級、只能掃 0050、沒有 PDF），是使用者明確要求「兩邊都保留」時留下的
snapshot，**沒有跟著這個專案之後的功能（反向篩選、top150、PDF）一起更新**。

---

## 2. 為什麼股票池是「0050 + 0051」而不是全部上市公司

使用者一開始要求「直接在上市公司中尋找標的」，也就是想掃描全部約 1000 檔上市
公司。但 FinMind 免費 API 有每小時請求次數上限（無 token 約 300 次/小時，
免費註冊 token 約 600 次/小時），全市場掃描一次可能要 1~3 小時，不適合塞進
Streamlit 網頁裡等待（尤其部署到 Streamlit Community Cloud 之後，雲端環境
通常有請求逾時限制，長時間運算容易被中斷）。

跟使用者討論後的折衷方案：改成「上市市值前 150 大」。這剛好可以用兩檔 ETF
的成分股湊出來，不需要另外算市值排名：
- 0050（元大台灣50）追蹤「臺灣50指數」＝ 市值前 50 大
- 0051（元大中型100）追蹤「臺灣中型100指數」＝ 市值第 51～150 名

兩者官方定義上互補不重疊，加總剛好是前 150 大，且 150 檔遠低於免費 API 額度，
一次掃描只需要幾秒到十幾秒。

## 3. 為什麼成分股清單是手動維護的靜態清單

FinMind 的 ETF 成分股資料集（`TaiwanStockActiveETFHolding`）需要付費 Sponsor
方案；證交所 OpenAPI 也查不到對應的免費端點；元大投信官方網站（yuantaetfs.com）
雖然有「申購買回清單」頁面會每日公布完整成分股，但是用 Nuxt.js 前端動態載入，
實際資料是透過內部 API（`/api/bridge`、`/api/trans` 之類，未進一步逆向）拉的，
沒有找到穩定、文件化的公開 JSON 端點可以直接串接。

因此比照公司基本面分析專案 `aliases_seed.py` 的做法，改用手動維護的靜態清單
（`etf0050_constituents.py` / `etf0051_constituents.py`）。查核方式：先用
WebFetch 讀玩股網（wantgoo.com，robots.txt 允許）的成分股頁面拿到完整名單，
再用該股票代號逐一比對是否出現在元大投信官方 PCF 頁面的原始 HTML（用 curl 抓
下來直接 grep 股票代號字串，不解析頁面結構，因為那是 client-side render 的
Nuxt payload，格式不穩定不適合寫程式解析）裡，兩邊都對得上才收錄，算是一種
「兩個獨立來源交叉驗證」的土法煉鋼查核法。

**這份清單需要每季（3/6/9/12 月）人工重新查核更新**，兩個檔案開頭都已經寫了
查核來源網址與方法，照著重跑一次就可以。

## 4. 均線分級邏輯：從 4 級到 8 級

最初版本只有多頭四級（四海遊龍／三陽開泰／短線翻多訊號／準備短線翻多）。
使用者後續要求加上對稱的空頭四級（四面楚歌／三聲無奈／短線翻空／注意停損），
邏輯是完全鏡像：多頭看「站上」幾條均線，空頭看「跌破」幾條均線，兩者都是
**巢狀判定**（例如「三陽開泰」代表站上 5/10/20MA，但不代表跌破 60MA——如果
也站上 60MA 會被歸類到更高一級的「四海遊龍」），每檔股票只會落在其中一級，
多空訊號不一致（例如站上 5MA 但跌破 10MA）的股票不列入這八個等級（`tier=0`）。

分級判斷寫在 `ma_screener.py` 的 `_classify_tier()`，先判多頭鏈（4→3→2→1），
四種都不成立才判空頭鏈（-4→-3→-2→-1），最後都不成立回傳 0。均線本身用
`moving_average.py` 的簡單移動平均（SMA），抓 6 個月股價資料當暖身（約可
涵蓋 100+ 交易日），確保 60MA 在最近一日一定算得出來（除非該股票掛牌不到
60 個交易日，這種情況目前沒有特別防呆，`ma60` 會是 `None`，該股票就無法進入
多頭 4 級或空頭 -4 級，但仍可能落入其他等級）。

## 5. PDF + HTML 雙輸出，以及 PC/iPhone 開啟方式

一開始只有 HTML 網頁版（`screen_page.py`），後來使用者要求要能在 PC 用瀏覽器
開啟並下載、iPhone 上也要能開啟/下載/分享，於是新增了 `pdf_report.py`
（ReportLab Platypus）產出 PDF，兩種格式內容一致，只是呈現形式不同：HTML
適合網頁上直接瀏覽（尤其 Streamlit 內嵌預覽），PDF 適合下載、分享、列印保存。

PC/iPhone 的開啟/下載/分享，直接照搬公司基本面分析專案已經驗證過的解法
（`app_streamlit.py` 裡的 `components.html` 區塊），沒有重新踩坑：
- **不要用 `data:` URI 做「開新分頁導覽」**：會被瀏覽器防釣魚機制擋下，結果
  是空白頁。改用 Blob URL（`URL.createObjectURL`）。
- **不要依賴 `<a download>`**：iPhone Safari 常常改觸發系統層級的 Quick Look
  預覽，把畫面整個蓋掉。改用 Blob URL + `window.open()`。
- 額外提供 Web Share API（`navigator.share`）當作「選擇開啟方式」按鈕，讓
  iPhone 使用者可以分享到其他 App（例如存到「檔案」）。

CJK 字型（`fonts.py`）也是照搬同一套跨平台偵測邏輯：本機優先用 Windows 內建
微軟正黑體，找不到時（雲端 Linux 主機）退回專案內建的 Noto Sans TC
（`assets/fonts/`，OFL 授權，這是為什麼這個「輕量」專案的 repo 裡也有一個
12MB 字型檔的原因——雲端部署一定要有，不能只靠本機系統字型）。

## 5.1 結果呈現：從「8 個區塊全部一起顯示」改成「下拉篩選只顯示一個等級」

最初版本（第 5 節）是 8 個等級的表格全部堆疊在同一頁，使用者要自己往下捲動找
想看的等級。使用者要求改成用下拉選單（`<select>`）一次只篩選顯示一個等級的
資料，選項順序照多頭到空頭排列（四海遊龍→三陽開泰→...→四面楚歌，即
`TIER_ORDER` 原本的順序，不需要改）。

做法是**純前端 JavaScript**，不是後端重新查詢：`screen_page.py` 產生 HTML 時
8 個 `<section class="tier" data-tier="...">` 全部都在，只是預設除了第一個
（四海遊龍）以外都有 `hidden` 屬性；下拉選單的 `change` 事件監聽器切換各
section 的 `hidden` 屬性，藉此做到「篩選成功後只剩該分類資料顯示」的效果，
不用重新產生 HTML 或重新整理頁面。這個實作直接寫在 `screen_page.py`（純
vanilla JS，沒有外部套件），好處是**同一份邏輯同時用在兩個地方**：獨立下載
開啟的 HTML 檔，以及 Streamlit 用 `components.html` 內嵌的那份——不用分開
實作一次 Streamlit 原生版本、一次靜態 HTML 版本。

連帶把 `app_streamlit.py` 原本「篩選完成後另外用 `st.metric` 顯示 8 個等級
檔數」的區塊拿掉了（下拉選單的每個選項文字裡已經有檔數，例如「四海遊龍
（26 檔）」，不需要在頁面上重複兩份）；也把內嵌 HTML 的 `components.html`
高度從 1400 降到 850，並在 `screen_page.py` 的表格加上 `max-height: 55vh` +
內部捲動（`overflow: auto`），因為現在畫面上一次只會有一個等級的表格，
不需要再預留「8 個表格全部展開」那麼多高度，但不同等級的檔數差異很大
（可能 1 檔到 60 幾檔），表格內部自己捲動比固定 iframe 高度更穩定。

## 5.2 欄位篩選：代號／收盤價／資料日期，為之後更複雜的篩選打基礎

使用者接著要求在「篩選分類」（哪個等級）之上，再加上針對個別欄位（代號、
收盤價、資料日期）的篩選，並明講「之後會有比較複雜的部份，篩選欄位必須先
做好」——也就是這次要求的重點不只是做出這三個欄位的篩選，還要把底層機制
設計成容易繼續加欄位。

做法：
- `_rows_html()` 幫每個 `<tr>` 加上 `data-stock-id` / `data-close` / `data-date`
  屬性（原始數值，不是格式化後的顯示字串，方便用數字比較收盤價區間）。
- 欄位篩選跟「篩選分類」（哪個 section 顯示）是**兩層獨立、疊加**的篩選：
  分類篩選控制 section 的 `hidden`；欄位篩選控制目前可見 section 裡面各
  `<tr>` 的 `hidden`。切換分類時兩者都會重新套用（`applyColumnFilters()`
  在 tier 的 `change` 事件裡也會被呼叫一次），欄位篩選的輸入值本身不會因為
  切換分類而被清空。
- 沒有任何一列符合欄位篩選時，會顯示一列「篩選條件無符合的個股」的提示列
  （`.no-match-row`），而不是留白讓人以為是頁面壞了。

**之後要加新的可篩選欄位（例如名稱、5MA 等）的擴充方式**：在 `_rows_html()`
的 `<tr>` 上加一個新的 `data-*` 屬性，在 `applyColumnFilters()` 裡加一個
對應的 `if` 判斷，並在欄位篩選工具列（`.column-filters`）加對應的
`<input>`——不需要更動「篩選分類」那一層的邏輯，兩層是解耦的。

## 5.3 改成 Excel 表單式的欄位篩選，並拿掉均線數值欄位

使用者接著要求兩件事：(1) 表格不要再顯示 5MA/10MA/20MA/60MA 這四欄數值；
(2) 篩選改成「類似 Excel 表單」的形式——欄位標題上有下拉篩選鈕，不要另外
一條獨立的篩選工具列。這代表原本「篩選分類（tier 下拉選單）+ 8 個各自獨立
的 section」的架構整個不再適用，改成完全重寫：**單一張表格**，欄位是
分類／代號／名稱／收盤價／資料日期（5 欄），每個欄位的 `<th>` 都有一個
篩選鈕，點下去展開一個核取方塊清單（勾選要顯示哪些值，有搜尋框可以在清單
內查找，清單最上面有「（全選）」可以整批勾/取消），行為模擬 Excel 的
「自動篩選」。

**篩選邏輯（`screen_page.py` 底部的 `<script>`）**：每個欄位維護一組「目前
勾選中的值」的 JS `Set`；一列資料要顯示，必須每個欄位都命中該欄位目前勾選
的集合才算通過——**欄位之間是 AND，同一欄位內的多個勾選值之間是 OR**，
這跟真正 Excel 自動篩選的語意一致。預設狀態是**全部勾選**（不篩選、顯示
全部資料），不像先前版本會預設只顯示「四海遊龍」——因為既然做成 Excel
風格，起始狀態就該跟 Excel 一樣是未篩選，由使用者自己勾選。

**分類（tier）也變成表格裡的一個一般欄位**：每列會有一個依多空方向上色的
徽章（`<span class="tier-badge">`，紅色系多頭、綠色系空頭，顏色深淺代表
訊號強弱，沿用原本 `_TIER_ACCENT`），分類欄位的篩選選項依 `TIER_ORDER`
（多頭到空頭）排序，不是照字母或出現順序，這樣下拉清單本身仍然保留「由
多頭到空頭」的閱讀順序。原本 8 個 section 各自的說明文字（`TIER_DESCRIPTIONS`）
沒有拿掉，改成放進一個預設收合的 `<details class="legend">`「各分類代表
意義」區塊，不佔用主要畫面空間，但需要時還查得到。

**為什麼是資料驅動、不是每欄位各寫一段程式**：Python 端 `_COLUMNS` 是一個
`(欄位 key, 表頭文字)` 的清單，`_row_attrs()` 統一產生每一列在每個欄位上的
「篩選比對字串」；JS 端讀 `_COLUMNS` 序列化過來的 `COLS` 陣列，用同一段
迴圈邏輯處理所有欄位的核取方塊事件、全選、搜尋、套用篩選，沒有針對特定
欄位寫 if/else。之後要加新的可篩選欄位（例如名稱以外的其他文字欄位，
或者以後真的要把 5MA 之類的均線數值重新加回來當篩選欄位），只需要：
1. `_COLUMNS` 加一筆 `(key, 表頭文字)`
2. `_row_attrs()` 補一個對應的 dict 項目
3.（如果排序方式不是「照出現順序」）在 `_unique_options()` 加一個排序規則
不需要動表頭產生、篩選面板產生、或 JS 篩選邏輯本身——這是使用者當初要求
「篩選欄位必須先做好」的具體實作方式。

**已知的簡化**：欄位 key 必須是純小寫（可以有連字號），因為直接拿去組
`data-{key}` HTML 屬性名稱；一開始用 `stockId`（camelCase）當 key 有踩到
一個潛在的坑——雖然 HTML 屬性名稱在解析後查詢時是不分大小寫的（規格上
`getAttribute` 在 HTML 文件裡本來就會把查詢字串轉小寫），理論上能動，但
為了不要依賴這個規格細節、避免之後換人維護時看起來像 bug，還是直接改成
`stock-id` 這種純小寫的 key，比較保險、也比較好讀。

## 5.4 篩選面板：加上「全部不選」，並修正面板被裁切看不到選項的問題

使用者回報兩個問題：(1) 想要一個明確的「全部不選」動作；(2) 取消全選後，
篩選面板「視窗過小，看不到選項」。

**問題 (2) 的根因**：篩選面板原本是 `position: absolute; top: 100%;`，
定位基準是它的父層 `<th>`，而 `<th>` 又在 `.table-wrap`（`overflow: auto;
max-height: 65vh;`）裡面。`.table-wrap` 沒有設固定高度，只有 max-height，
所以它的實際高度是跟著目前顯示的資料列數量走的。當使用者把某個欄位篩到
只剩很少列（極端情況是全部不選、只剩下一列「無符合資料」的提示列）時，
`.table-wrap` 的實際高度會縮到只剩一兩行文字高，而面板是這個容器的
`overflow` 子元素，容器矮下去之後，面板凡是超出容器範圍的部分就會被直接
裁掉——不是面板本身變小，是面板被「切掉」了，看起來才會像「視窗過小」。

**修法**：面板改成 `position: fixed`，並且在每次要打開面板時用 JS
（`positionPanel()`）即時抓觸發按鈕的 `getBoundingClientRect()`，把面板的
`top`/`left` 設成相對於整個瀏覽器視窗的座標，而不是相對於會縮放的
`.table-wrap`。`position: fixed` 的元素本來就不受任何祖先層 `overflow`
裁切影響（除非祖先層有 `transform`，這裡沒有），從機制上直接排除了「容器
縮小連帶裁到面板」這整類問題，而不只是頭痛醫頭把 `.table-wrap` 撐高。
順便加了「空間不夠就自動往上/往左翻」的判斷，避免面板在畫面邊緣（尤其
Streamlit 內嵌的 iframe 高度只有 850px，很容易貼近邊緣）超出可視範圍。

**問題 (1)**：原本只有一個「（全選）」核取方塊，取消勾選它會連帶把底下每個
選項都取消勾選，等於已經有「全部不選」的效果，只是要使用者自己理解「取消
全選 = 全部不選」這層意思，不夠直覺。改成兩個並排、語意明確的按鈕「全選」
／「全部不選」，拿掉原本容易被誤會的核取方塊。這兩個按鈕都只作用在**目前
搜尋框篩出來、看得到**的選項上（不影響被搜尋隱藏起來的其他選項），這跟
Excel 篩選清單本身的行為是一致的。

## 5.5 「各分類代表意義」按鈕放大，以及篩選面板內搜尋框失效的 bug

**圖例按鈕放大**：`<details class="legend"><summary>` 原本字級只有
0.82rem、純文字沒有任何按鈕感，使用者反映不夠明顯。改成 1rem、粗體、
白底圓角膠囊（pill）造型，加上陰影跟一個會隨展開/收合旋轉的 ▾ 箭頭
（`details.legend[open] summary::after { transform: rotate(180deg); }`），
明顯是個可以點的按鈕。

**篩選面板內搜尋框失效（已修正的 bug）**：使用者回報「單一類別的關鍵字
搜尋沒有生效」。根因是 CSS 特異度（specificity）衝突：
`.filter-options label { display: block; ... }` 這條規則的特異度
比瀏覽器內建的 `[hidden] { display: none; }` 規則還高，所以當 JS 把某個
不符合搜尋字串的 `<label>` 設成 `hidden = true` 時，畫面上其實**沒有**
真的消失——`display: block` 蓋掉了 `[hidden]` 該有的 `display: none`，
使用者看到的就是「打了關鍵字，清單完全沒變化」。

修法：另外加一條特異度更高的規則 `.filter-options label[hidden] {
display: none; }`（比原本那條多了一個屬性選擇器 `[hidden]`，特異度
`(0,2,1)` > `(0,1,1)`，會蓋回去），不需要拿掉原本 `display: block` 那條
（那條是負責讓「有顯示」的 label 保持區塊排列跟省略號截斷效果）。

**教訓**：任何元素只要同時（1）用 `hidden` 屬性/`.hidden` 屬性做顯示切換、
又（2）在自己的 CSS 裡對同一個元素明寫了 `display: ...`，就要檢查一下
特異度有沒有蓋掉 `[hidden]` 預設的 `display: none`——這個專案裡目前只有
`.filter-options label` 踩到這個坑（用 grep `display:` 找過整份檔案，
`tr`、`.filter-panel`、`section` 等其他靠 `hidden` 切換顯示的元素都沒有
被自己的 CSS 蓋掉，不用比照修改）。

## 5.6 搜尋框沒有連動套用到表格（第二個 bug，跟 5.5 的 bug 是兩回事）

修完 5.5 那個「搜尋框視覺上沒反應」的 CSS 特異度 bug 之後，使用者接著發現
第二個問題：搜尋框現在會正確縮小面板裡的核取方塊清單了，但**表格資料沒有
跟著變**。這是兩個各自獨立的 bug，不是同一個問題的殘留。

**根因**：搜尋框的 `input` 事件監聽器，原本只做了「把不符合搜尋字的
`<label>` 設成 `hidden`」這件事，從頭到尾沒有呼叫 `applyFilters()`；而
`applyFilters()` 本身判斷一列要不要顯示，也完全沒有把「目前搜尋框打了
什麼字」納入考慮，只看 `selected[col]`（核取方塊目前的勾選集合）。也就是
搜尋框在設計上從一開始就只是「幫忙在核取方塊清單裡找到想要的選項」的
輔助小工具，跟「表格實際顯示哪些資料」完全是兩條不相干的邏輯路徑——這不是
單一行程式碼打錯字的筆誤，是當初這個功能的實作範圍就沒有涵蓋「搜尋要直接
連動篩選表格」這件事。

**修法**：讓搜尋框變成一個真正的即時篩選條件，而不只是清單縮小小工具。
每個欄位除了原本的 `selected[col]`（勾選集合）之外，新增一個
`searchQuery[col]`（目前這個欄位搜尋框打的字，預設空字串）。
`applyFilters()` 判斷一列要不要顯示時，改成「這個欄位的值必須同時
(1) 在勾選集合裡、且 (2) 符合這個欄位目前的搜尋字（沒打字就不做這項
限制）」，兩個條件都要通過。搜尋框的 `input` 事件除了原本縮小清單的
邏輯以外，也把 `searchQuery[col]` 更新掉、呼叫一次 `applyFilters()`，
這樣打字的當下表格就會即時跟著篩選，不用像原本設計那樣還要額外去勾選/
取消勾選核取方塊才會反映到表格上。

**驗證方式與後續回報**：5.6 的修法上線（commit `777dfd0`）後，使用者一度
又回報同樣的症狀（「分類」欄位搜尋「四海」，表格卻還顯示三陽開泰等其他
分類）。這次沒有直接改程式碼，而是先**用 Node.js + jsdom 實際載入當時最新
產生的 HTML、模擬在搜尋框 dispatch 一個真正的 `input` 事件，再檢查 DOM
裡實際哪些 `<tr>` 是 `hidden`**——不是用眼睛讀程式碼猜邏輯對不對。結果
`tier`／`stock-id`／`name`／`close`／`date` 五個欄位的搜尋框全部都正確
連動篩選表格，包含「搜尋字完全比對不到任何資料」時會正確顯示無符合資料
的提示列。結論是 `777dfd0` 這個修正本身沒有問題，使用者當時看到的舊行為
是**測試到修正前產生的舊報告**（本機 Streamlit 沒有重新按「開始篩選」重新
產生報告，或是打開了修正前下載存下來的舊 `.html` 檔案）。

**這個插曲值得記錄的教訓**：這個專案的核心邏輯全部是產生出來的靜態 HTML +
vanilla JS，不是持續運行的後端服務——每次「使用者說某個互動行為壞掉」時，
第一步應該先確認是不是在測試舊版輸出（重新整理頁面／重新按開始篩選／重新
下載），而不是直接假設程式碼有新 bug 就去改。有 Node.js 可用時，用 jsdom
實際跑一次 HTML+JS 再看 DOM 結果，比單純讀程式碼推論可靠很多，尤其是這種
牽涉多個事件監聽器交互作用的篩選邏輯，之後如果還有類似「看起來邏輯對但
行為不對」的回報，可以比照這個方式先驗證再動手改。

## 5.7 放大欄位篩選面板（含核取方塊本身），手機上更好點選

在「篩選器APP」手機殼開發過程中，使用者實測手機版時反映篩選面板點選困難，
要求放大。這是共用元件（`_table_header_html()` 產生的欄位篩選面板），
Streamlit 網頁版跟 App 殼都會受影響，兩邊一起變好點，不是只改給 App 用。
面板寬度 14rem→17rem、選項清單可視高度 12rem→16rem、每個選項的字級跟
上下留白加大、**核取方塊本身也放大**到 1.2rem（原本是瀏覽器預設大小，
手機上最容易點歪的就是這個，只放大文字/行高不夠）。`positionPanel()`
的動態定位邏輯本來就是在開啟當下讀面板實際尺寸再算 top/left（見 5.4
節），面板變大不需要額外調整這段邏輯。

## 6. iPhone「加入主畫面」：只做了 PWA meta 標籤，沒有做自訂圖示

使用者問過「怎麼做成 iPhone App」，說明過兩條路：
1. PWA 方式（Safari 分享 → 加入主畫面）：不需要 Apple 開發者帳號、不用審核，
   現在就能用。
2. 真正上架 App Store：要重寫 Swift/SwiftUI 前端、要 Apple Developer Program
   年費、要送審，工程量完全不同等級，個人工具不划算。

使用者選了方式一，並且明確說「LOGO 目前先不需要」。因此目前只加了
`apple-mobile-web-app-capable`、`apple-mobile-web-app-title`、`theme-color`
這幾個 meta 標籤（讓加入主畫面後開啟是全螢幕獨立模式，沒有 Safari 網址列），
**沒有**做自訂的 favicon/apple-touch-icon，圖示維持 Streamlit 預設的 📈 emoji。

如果之後要做自訂圖示，公司基本面分析專案已經踩過這個坑：iOS 讀取圖示的時機
可能比 `st.markdown(unsafe_allow_html=True)` 注入 `<link>` 標籤的 JS 執行
時機更早，導致圖示不保證每次都套用成功；更徹底的做法（直接覆寫 Streamlit
套件內建的 `static/index.html`）在 Streamlit Community Cloud 上會因為
`site-packages` 唯讀而失敗（`PermissionError`）。這是 Streamlit 平台本身的
限制，不是這個專案獨有的問題。

## 7. 部署

Repo 一開始建立時是 private，後來為了能在 Streamlit Community Cloud 部署
（跟公司基本面分析專案一樣的教訓：private repo 在 Streamlit Cloud 的
「選擇 repo」選單裡選不到），改成了 **public**（已確認程式碼裡沒有任何密碼
/token 等敏感資訊——密碼保護是透過 Streamlit Cloud 的 Secrets 功能設定
`APP_PASSWORD`，不會出現在程式碼或 git 歷史裡）。

部署步驟見 [README.md](README.md) 的「使用方式」段落。Main file path 是
`etf_screener/app_streamlit.py`。

### 7.1 「手機上的 App 內容是不是最新版」要怎麼確認

Streamlit Community Cloud 會在偵測到 GitHub 有新 push 時自動重新部署，
通常一兩分鐘內完成，**不需要手動觸發**；每次改完程式碼、確認 `git push`
成功，理論上手機上（PWA 加到主畫面那個）過一下子就會是最新版。這裡沒有
CLI/API 可以直接從這個開發環境檢查或觸發 Streamlit Cloud 的部署狀態
（不像 GitHub 有 `gh` 這種官方 CLI 工具），只能請使用者自己到
share.streamlit.io 後台確認。

如果過了幾分鐘，手機上看起來還是舊版，依序排查：
1. **雲端後台**：登入 share.streamlit.io，看 App 的「⋮」選單裡最後部署
   時間／狀態，卡住的話手動點 **Reboot app**（尤其是這個 session 中途
   新增了 `reportlab` 依賴，第一次要裝新套件時比較容易需要手動重啟一次）。
2. **手機端**：加到主畫面的 PWA 有自己的快取，跟一般開瀏覽器分頁不一定
   同步。先把捷徑完全關閉（不是切背景）再重開；還是舊的話，到「設定 →
   Safari → 進階 → 網站資料」找到該網址、移除資料後再重新打開一次——這個
   坑公司基本面分析專案在處理 apple-touch-icon 快取時就踩過一次（見該
   專案的 DEVELOPMENT_LOG.md），道理相同：iOS 對加到主畫面的頁面快取比
   一般分頁更頑固。

## 10. 響應式改版：從「網頁內嵌一份固定內容」變成「像 APP 一樣的側邊選單」

使用者提出「兩個專案是否都能做成響應式網頁」的討論後，決定先在這個專案試做：
手機版把篩選設定／PDF／網頁下載這些操作收進畫面左側可滑出的選單，預覽區改成
貼齊螢幕邊緣的滿版顯示，行為更接近一般 APP 而不是「網頁裡嵌了一個 iframe」。

**首頁的互動流程改了三輪，最後收斂成「標題 → 選單 popover → 說明」**：
- 一開始是 `st.sidebar` 裡放 radio + 按鈕，但使用者發現這樣「篩選設定」在還
  沒篩選過之前也被收在側邊欄，第一次進來要多一次點擊才看得到選項，體驗上
  不如原本首頁直接看到選項好。
- 改成：**還沒有結果時**，選項用 `st.popover("下拉選擇篩選內容")` 直接放在
  主畫面上，popover 裡是每個股票池各自一個按鈕，點下去就直接觸發
  `screen_fn()`（不像原本的 radio + 送出按鈕要兩步），選完立刻開始跑；
  **有結果之後**，同一組按鈕改成搬進 `st.sidebar`，因為這時候主要畫面該
  讓給篩選結果，重新篩選變成次要動作，適合收進側邊欄「需要時才點開」。
- PDF 報告下載（Blob URL 開啟／分享按鈕）跟網頁下載（`st.download_button`）
  也一併整合進側邊欄，不再散落在主頁面中間擋住篩選結果的閱讀動線（原本這兩
  組按鈕是直接放在結果表格上方，使用者反映「跑到了畫面中間，擋住了原本該
  瀏覽的項目」）。

**首頁 popover 按鈕跟側邊欄選單「慢半拍」的 bug 與修法**：`screen_result is
None` 這個條件原本是拿來決定「選單要畫在主頁面還是側邊欄」的唯一依據，但
點擊按鈕、`screen_fn()` 執行完、`session_state` 更新，這些全部發生在**同一次
腳本執行**裡，條件判斷式在那次執行裡讀到的還是舊值，所以剛篩選完那一次畫面
選單還留在主頁面，要等下一次互動（例如展開圖例）才會「跳」到側邊欄。修法是
在更新完 `st.session_state["etf_screen_result"]` 後立刻呼叫 `st.rerun()`，
強迫用新的 session state 重新跑一次整支腳本，讓分支判斷讀到的一定是最新值。

**iframe 高度自動撐開的循環依賴 bug**：`components.html(screen_html, ...)`
原本用固定高度（850px），使用者要求改成自動貼合內容高度，不要留一大塊空白也
不要外層再多一條捲軸。`screen_page.py` 原本用 `resizeFrame()`（讀取
`document.body.scrollHeight` 回報給外層調整 iframe 高度）搭配 CSS
`max-height: 65dvh`（`dvh` 是相對「iframe 自己的高度」算的），這兩者疊在一起
會變成循環：表格的可視高度用 iframe 自己的高度去算 → iframe 的高度又是用
表格（連同其他內容）的 scrollHeight 去撐開 → 越撐越大或高度抖動不穩定。
修法是**不要用 `dvh`，改成 JS 主動去讀外層真正的可視高度**：
`computeTableMaxHeightPx()` 透過 `window.parent.innerHeight`（同源 iframe 才
拿得到，這裡因為 `components.html` 產生的 iframe 跟 Streamlit 主頁同源，可以
這樣讀）算出一個以 CSS 變數 `--table-max-h` 表示的像素值餵給表格，這個數字
不會因為表格自己的高度變化而反過來被影響，打破了循環依賴。

**手機版全螢幕滿版顯示**：使用者確認整體「頗有 APP 的風骨」後，進一步要求
「搜尋內容」（篩選結果表格）也要滿版顯示，不要有明顯的左右留白。做法是
`.main .block-container` 在 `@media (max-width: 640px)` 底下把內距壓到接近
0，並且讓 `.table-wrap` / `.chart-view`（後面第 11 節新增的技術分析圖表容器）
用**負的左右 margin** 抵消掉外層還殘留的一點內距，做到真正貼齊螢幕邊緣的
效果；同時把表格可用視窗高度佔比從 60% 調高到 80%，手機直向瀏覽時可以一次
看到更多列。

**側邊欄互動的其餘細節**：
- `st.set_page_config(..., initial_sidebar_state=...)` 最後固定成
  `"collapsed"`——篩選完成後側邊欄應該自動收合、需要時使用者自己點開，不要
  每次重新整理都預設展開擋住畫面。
- 篩選完成後，用一個 `st.session_state["_collapse_sidebar_next"]` 旗標搭配
  `components.html` 注入的 JS 片段（`window.parent.document.querySelector`
  找到側邊欄的收合按鈕並模擬點擊）做「盡力而為」的自動收合——這是 best-effort
  的做法，不保證每次都成功（取決於 Streamlit 內部 DOM 結構、時序），但多數
  情況下可以讓使用者篩選完不用自己手動再點一次收合。
- 側邊欄的收合切換按鈕（`[data-testid="stSidebarCollapseButton"]`）原本很不
  明顯，使用者反映找不到，改成紅色圓形膠囊、放大圖示，讓「這裡可以點開選單」
  這件事一眼就看得出來。
- 首頁選單按鈕（popover 觸發鈕）前後放大了三輪：先是整體變大，後來使用者
  再要求「方格可以呼應字體數量，不用這麼大的框框，並加大一點顯示」，改成
  按鈕大小貼合文字內容（不留過大的空白框），同時把按鈕文字改成更明確的
  「下拉選擇篩選內容」，並置中顯示。

## 11. 技術分析圖表：點收盤價互動查看 K 棒＋均線＋KD

使用者評估過「點股票代號產生基本面報告」跟「點收盤價產生技術分析圖表」兩個
方向後，選了後者先做，並且明確要求「產出線圖後要能有返回至均線分析結果頁面
的設計」——也就是圖表不能是另開一頁或另一份輸出，必須跟篩選結果表格共存在
同一份自包含 HTML 裡，可以來回切換。

**資料模型**：`ma_screener.py` 的 `MaScreenRow` 原本只留「最近一日」的
ma5/ma10/ma20/ma60 數字，為了畫圖需要整段暖身期間（約 6 個月）的完整每日
序列，新增了 `history_dates` / `history_close` / `history_open` /
`history_high` / `history_low` / `history_ma5~ma60` / `history_k` /
`history_d` 這些 `list` 欄位，長度互相一致、同一個 index 對應同一個交易日。
KD 指標另外拆成新檔案 `kd.py`（`compute_kd()`），沿用台股慣用的 9 日 RSV +
2/3、1/3 平滑法，跟均線計算（`moving_average.py`）分開放，維持每個技術指標
各自一個檔案的慣例。這些 history 欄位只有畫圖會用到，PDF／既有表格不受影響。

**沿用「同一份 HTML 兩種用途」的既有架構**：`screen_page.py` 把每檔股票的
`price_history`（含以上所有序列，經 `_round_series()` 統一四捨五入）序列化成
`PRICE_HISTORY` 這個 JS 物件內嵌進 HTML；表格的收盤價儲存格從純文字改成
`<button class="price-link" data-stock-id="..." data-name="...">`，點擊後
用純前端 JS（`openChart()`）切換顯示 `#tableView` / `#chartView` 兩個區塊
（`hidden` 屬性互斥），**沒有任何後端往返**，圖表資料在第一次產生 HTML 時
就已經全部帶著走，這跟本專案一貫「同一份 vanilla JS 邏輯同時服務 Streamlit
內嵌預覽跟獨立下載檔案」的原則一致，返回按鈕就是把 `hidden` 切回去，不涉及
瀏覽器歷史或重新整理。

**圖表渲染是手刻 Canvas 2D，不依賴外部圖表套件**：`drawPanel(canvas, dates,
seriesList, hoverIdx, opts)` 是價格圖跟 KD 圖共用的繪製函式，用選項物件區分
兩者差異（`opts.range` 目前縮放範圍、`opts.fixedRange` KD 固定 0~100 區間、
`opts.ohlc` 是否畫 K 棒、`opts.gridValues`/`opts.refLines` 格線與參考線等），
避免價格圖跟 KD 圖各寫一份幾乎重複的畫圖邏輯。均線/K棒/KD 的 K 線 D 線都可以
用 `#chartLegend` 底下的按鈕個別開關（`seriesVisible` 這個 JS 物件記錄目前
狀態，`renderChart()` 每次都依它過濾要畫的 series），另外加了一個「KD 全部」
的群組按鈕，一次關掉/開啟 K 線＋D 線兩條（同時操作 `.kd-hidden` 讓整個 KD
子圖區域收合，不是只是隱藏線條本身，省下的垂直空間會還給價格圖）。

**收盤價：先用折線，後改成 K 棒**：第一版查價互動先用折線圖驗證整體查價/
縮放/平移的架構可行，使用者確認體驗方向沒問題、也加上 KD 子圖後，才明確
要求「收盤價的部分，我想還是改成 K 棒標示收盤價比較習慣」——這是使用者對
台股看盤介面的既有習慣，K 棒（`fillRect` 畫實體、直線畫上下影線，紅漲綠跌）
比單純折線傳遞更多資訊（開高低收），改動集中在 `drawPanel()` 的 `opts.ohlc`
分支，其餘縮放/平移/查價邏輯不受影響。

**查價線的兩種模式與標示內容**：使用者要求保留原本「五個點（各條均線）同時
移動」的查價模式，並新增一個「單一十字線，只標示在收盤價上」的模式，兩者
可以切換（`crosshairMode` 為 `'multi'` 或 `'single'`，切換用
`#chartModeBtn`）。無論哪個模式，查到的那一天要清楚標出「目前在看哪一天、
收盤價多少」——一開始只在游標處畫了收盤價數字（`opts.showHoverLabel` 的
浮動標籤，用 `ctx.roundRect`，沒有原生支援的瀏覽器退回 `ctx.rect`），
使用者接著回報「有標記收盤價格，但沒有橫線很不習慣」，因為看盤軟體慣例上
十字線除了直線指到日期，還會有一條橫線指到對應的價格刻度，只給數字沒有線
反而不容易對齊——補上了以收盤價 Y 座標為準的水平參考線（只有 `opts.ohlc`
存在、也就是價格圖本身才畫，KD 子圖不需要）。

**縮放/平移的資料模型**：`chartRange = {start, end}` 是目前可視窗格對應到
`PRICE_HISTORY` 陣列的索引區間，`applyZoom(factor, anchorIdx)` 依游標/觸點
位置為錨點縮放、`applyPan(daysDelta)` 平移，`clampRange()` 統一收斂邊界。
互動輸入源有三種都要converge到同一組函式：滑鼠滾輪（垂直滾動=縮放、
`deltaX` 水平滾動=平移）、雙指觸控 pinch（`touchDistance()` 算兩指間距變化
=縮放比例，兩指中點同時位移=平移，兩者可以同時發生）、以及下面提到的單指
手勢判斷。

**「放大到較早日期後，今天的資料被遮蔽在視窗外」的可用性問題**：使用者示範
的情境是查價線移到 7/27 附近再放大，這時候 `chartRange` 的視窗中心跟著查價
位置走，最新一天（8/31）自然被擠出可視範圍，要使用者自己再手動縮小才看得到
——這其實是「以查價位置為錨點縮放」這個設計本身的必然結果，不是計算錯誤。
解法不是改變縮放行為（那是使用者要的功能），而是新增一個「跳到最新」按鈕
（`#chartJumpLatestBtn`），偵測到目前視窗右邊界不是資料最後一天時就顯示
出來，一鍵把 `chartRange` 重新對齊到最新日期，讓使用者可以「先仔細看某一天
附近的細節，再一鍵跳回目前」，而不用兩者只能擇一。

## 12. 手機觸控手勢：兩階段除錯，第二階段才是真正根因

使用者確認技術分析圖表在電腦上體驗不錯後，回報「電腦端不會遇到，單在手機
執行上會遇到的問題是：開啟技術線圖時，放大會因為畫面版面問題，造成無法上下
滑動，造成無法返回上一頁」。這個 bug 分兩階段排查，第一次修的不是真正根因，
是使用者用更精確的描述（「都需要滑畫面邊邊才可以上下移動」）才點出問題所在。

**第一階段（合理但不是根因的修法）**：一開始假設是瀏覽器原生的雙指縮放
（pinch-zoom）沒有被限制在圖表區域內，蔓延到整個頁面，導致頁面版面跑掉、
捲動跟著失控。修法是在外層 Streamlit 頁面（`app_streamlit.py`）跟
`screen_page.py` 自己的 `<body>` 都加上 `touch-action: pan-x pan-y`，並把
viewport meta 改成 `maximum-scale=1, user-scalable=no` 全面關掉原生頁面
縮放。這個修法本身沒有錯、也保留下來（避免使用者不小心整頁被縮放），但
使用者後續回報「目前遇到的是......」以及更關鍵的下一句，指出問題還在。

**第二階段（真正根因）**：使用者精確描述「像我的手機是 iPhone 17 Pro Max，
都需要滑畫面邊邊才可以上下移動」——這句話直接點出圖表 canvas 幾乎佔滿手機
螢幕垂直空間（前一節提到手機版表格/圖表可用高度佔比拉高到 80%），而
canvas 的觸控事件監聽器（`touchmove`）原本**不分方向**，只要是單指觸摸在
canvas 上移動就一律當成「拖曳查價/縮放手勢」處理並呼叫
`event.preventDefault()`，這會連帶擋掉瀏覽器原生的垂直捲動——使用者要往下
滑捲動頁面，只要手指落在佔滿螢幕的圖表區域內，捲動就會被吃掉，只有畫面邊緣
沒被 canvas 蓋到的窄條才滑得動，跟使用者的描述完全吻合。

**修法**：
1. `.price-canvas` / `.kd-canvas` 的 CSS `touch-action` 從 `none`（完全交給
   JS 處理、瀏覽器不介入任何觸控手勢）改成 `pan-y`（瀏覽器原生垂直捲動
   優先放行，其餘手勢才交給 JS）。
2. 在 JS 裡加入**手勢意圖判斷**：`touchstart` 先記錄起始座標
   （`touchStartX/Y`），不立刻決定這次觸控是要捲動頁面還是操作圖表；直到
   `touchmove` 累積位移超過 `TOUCH_INTENT_PX = 8` 像素的門檻，才依水平/垂直
   位移量哪個比較大來分類：**水平位移為主 → 判定為「操作圖表」**（呼叫
   `preventDefault()` 接管、開始查價/平移邏輯），**垂直位移為主 → 判定為
   「頁面捲動」**（完全不呼叫 `preventDefault()`，讓瀏覽器原生捲動接手，
   JS 這次觸控之後也不再介入）。位移量沒超過門檻的輕觸（tap）則在
   `touchend` 時當成一次查價定位，不影響原本點一下看某天數值的操作。
3. 這個門檻判斷只在每次觸控手勢開始時做一次分類，分類結果在整個手勢期間
   固定，不會出現「捲到一半突然被圖表接管」或反過來的中途切換體驗。

**驗證方式**：沒有瀏覽器自動化工具可用（`chromium-cli` 找不到、也沒裝
Playwright），改用 Node.js + jsdom 在 scratchpad 暫存資料夾模擬合成的
`touchstart`/`touchmove` 事件（垂直位移為主、水平位移為主兩種情境各測一次），
檢查事件物件的 `defaultPrevented` 是否符合預期（垂直手勢應為 `false`、
水平手勢應為 `true`）；實際手勢是否順手，最終仍是使用者在自己的 iPhone 17
Pro Max 上實測確認（「目前已經正常了」）。

**教訓**：使用者第一次描述「放大會造成無法上下滑動」時，看起來直接對應到
「原生縮放蔓延」這個猜測，修完後問題卻沒解決；第二次使用者換了更具體的
描述方式（要滑「畫面邊邊」才捲得動），才讓真正機制（canvas 觸控事件搶走了
整個垂直手勢，不分方向）浮現出來。手機端的觸控/手勢類 bug，使用者對「哪裡
還能滑、哪裡滑不動」這種具體到像素區域的描述，往往比「放大會怎樣」這種功能
層級的描述更能定位根因，之後遇到類似回報可以主動追問這類細節。

## 12.1 查價十字線標籤：只跟著日期（水平）動，沒跟著價格（垂直）動

使用者測試「篩選器APP」手機殼時回報：K 線圖查價十字線的日期/收盤價標籤
只有水平方向會跟著手指移動，垂直方向卻固定貼在圖表最上面，不管收盤價
高低都不會跟著十字線的交叉點移動。

**根因**：`drawPanel()` 裡畫這個標籤時，`boxX`（水平位置）本來就是用
`hx`（十字線的 X 座標）算出來的，但 `boxY` 直接寫死等於 `padTop`（繪圖區
最上緣），完全沒有參考十字線橫線所在的 Y 座標（`hy`，收盤價換算出來的
高度）。`hy` 其實在同一個函式稍早（畫十字線橫線那段）就已經算好了，
只是沒有拿來用在標籤定位上。

**修法**：`boxY` 改成優先貼在交叉點正上方一點的位置（留 6px 間距，不
直接蓋住十字線的圓點），空間不夠（交叉點太靠近頂端）才自動改貼下方，
並整體限制在繪圖區內不會超出圖表範圍。用真實資料重新產生過 HTML、抽出
內嵌的 JS 做語法檢查，也單獨驗證過位置計算在「價格在中間／貼近頂端／
貼近底部」幾種情況下都正確落在圖表範圍內——這部分是 Canvas 繪圖，這個
環境沒有瀏覽器自動化工具，實際視覺效果仍是使用者在手機上確認過才算數，
跟第 12 節同樣的驗證限制。

---

## 13. 已知限制（目前沒有解法，非 bug）

- 成分股清單（0050+0051）是靜態資料，每季換股後有空窗期需要人工更新，見第 3 節。
- 「上市市值前 150 大」是用兩檔 ETF 成分股近似，不是每日重新計算真實市值排名，
  剛好卡在第 150 名附近、還沒反映在 ETF 換股上的股票可能有落差。
- 只驗證過上市股票的官方股價備援（證交所 STOCK_DAY），0050/0051 成分股皆為
  上市股票，不受影響，但如果之後把股票池換成含上櫃股票的清單，備援會不可用
  （這點跟公司基本面分析專案的已知限制一樣）。
- iPhone「加入主畫面」的自訂圖示還沒做（見第 6 節），目前用預設 emoji 圖示。
- 均線分級（5/10/20/60 日、八個等級的巢狀判定）是常見技術分析框架，非官方
  統一標準，也不構成任何買賣建議。
- 為了解決第 12 節的觸控 bug，全站（含獨立下載的 HTML）已經關閉瀏覽器原生的
  雙指頁面縮放（`user-scalable=no` + `touch-action` 限制），這是刻意的取捨：
  換取圖表手勢跟頁面捲動不互相干擾，但也代表想放大整個頁面文字（不是圖表
  本身）的使用者，現在只能靠系統層級的螢幕放大功能，網頁本身不再提供這個
  管道。
- 這個開發環境沒有瀏覽器自動化工具（`chromium-cli`、Playwright 皆未安裝），
  技術圖表跟觸控手勢類的改動只能靠 jsdom 模擬 DOM/事件、`py_compile`／直接
  執行 Python 產生 HTML 做基本檢查，實際視覺呈現與手勢是否順手，最終都要
  依賴使用者在真實瀏覽器/手機上測試回報，見第 12 節的除錯過程。

## 14. 行動 App 化：新增 API 層，衍生出獨立的「篩選器APP」專案

使用者評估過「直接用 Flutter/原生重寫」跟「用 Capacitor 把現有網頁包成手機殼」
兩條路之後（前者要把 `screen_page.py` 手刻的 Canvas 圖表、Excel 風格欄位篩選、
第 12 節那套觸控手勢意圖判斷全部重寫一次，估算 3-5 週；後者這些邏輯幾乎能
照搬，估算 1-1.5 週），選了 Capacitor。這一節記的是**這個決定在「篩選器」
專案裡留下的唯一異動**——新增一層 HTTP API，讓手機 App 可以打；手機殼本身
（Capacitor 專案）**不放在這裡**，是刻意拆到旁邊獨立的
`../篩選器APP/` 資料夾，做法與理由見該專案自己的 `DEVELOPMENT_LOG.md`。

**為什麼 API 只加不改**：手機 App 沒辦法像 Streamlit 一樣直接在同一個 Python
process 裡呼叫 `ma_screener.py`，需要一個獨立跑、能被 HTTP 打到的服務。新增
`etf_screener/api.py`（FastAPI）跟 `requirements-api.txt`（額外的
`fastapi`/`uvicorn`，不動 `requirements.txt`，因為 Streamlit 版本身不需要
這兩個套件），直接 import 既有的 `screen_0050`/`screen_top150`/
`render_screen_html`/`render_screen_pdf`，跟 `app_streamlit.py` 是兩個平行的
前端入口，共用同一套底層邏輯，沒有複製任何篩選/報告產生程式碼。

**三個端點**：`GET /health`、`GET /screen/{universe}`（回傳 JSON：
`universe_label`/`generated_at`/`as_of_date`/`total_count`/`skipped_count`/
`html` 自包含 HTML 字串）、`GET /screen/{universe}/pdf`（回傳 PDF 二進位檔）。
密碼保護沿用 `APP_PASSWORD` 環境變數（跟 `app_streamlit.py` 共用同一個變數
名稱、同一套「沒設定就不擋」的邏輯），改用 `x-app-password` header 傳遞。

**加了一層結果快取（`_result_cache`，TTL 10 分鐘）**：如果沒有這層，同一次
「使用者在 App 裡先看表格、再按 PDF」的操作會觸發兩次完整的 `screen_fn()`
（重新逐檔查價+分類）。雖然個股價格本身已經有 `cache.py` 的 4 小時 TTL 快取
擋著、不算太貴，但兩次獨立呼叫之間如果剛好跨過價格快取過期的瞬間，會導致
使用者在同一次操作裡看到的 HTML 表格跟 PDF 報告「資料日期」對不起來——直接
在 API 這層把整個 `MaScreenResult` 物件快取起來，兩個端點在同一個快取視窗內
保證用同一份底層資料。

**發現並修好一個 bug（PDF 檔名含中文導致 500）**：`Content-Disposition` 這個
HTTP header 的值規格上只能是 latin-1，`filename="0050成分股均線篩選_....pdf"`
這種含中文字的檔名直接塞進去會在 `Response.init_headers()` 丟
`UnicodeEncodeError`，本機測試（用真實 FinMind 資料實際打這個端點）才發現，
不是憑空猜測。修法是同時給兩個 `Content-Disposition` 參數：ASCII 安全的
`filename`（純英文＋日期，當作看不懂 `filename*` 語法的舊客戶端的備援）加上
RFC 5987 語法的 `filename*=UTF-8''<percent-encoded>`（真正含中文的檔名，
`urllib.parse.quote()` 編碼），兩者並存是這個 RFC 建議的標準寫法。

**已知風險：中途下錯 `taskkill` 指令**：本機測試時用過一次
`taskkill /F /IM python.exe /T`，這個指令沒有限定範圍，會把當下系統上**所有**
python.exe 行程關掉，不只是測試用的 uvicorn。之後已經改成先用
`netstat -ano | grep LISTENING` 找出正確 PID 再精準關閉那一個行程，如果之後
還要在本機跑多個 Python 服務做測試，記得比照後者的做法，不要圖方便用
`/IM` 整批關。

## 14.1 部署到 Render 後才現形的兩個證交所備援 bug

API（`etf_screener/api.py`）部署到 Render 之後，使用者實際在手機 App 上跑
篩選時發現大量個股查詢失敗。追下去發現：**FinMind 從 Render 的 IP 打過去
會回傳 402 Payment Required**（直接用 curl 從這個開發環境打同一個網址是
正常的 200，證實是 IP 層級的問題，不是資料集本身要收費，也不是我們程式
的 bug，見 14.2）。這個發現本身意外地讓「證交所 STOCK_DAY 備援」這條路
第一次真正被大量執行到——在此之前 FinMind 幾乎都成功，備援路徑很少被
用到，兩個一直潛伏著的 bug 也才第一次現形：

1. **`http_client.is_allowed_by_robots()` 的 robots.txt 判斷邏輯反了**：
   證交所網站的 SSL 憑證缺少 Subject Key Identifier 這個擴充欄位，導致
   Python 內建 `urllib`（`robotparser` 用這個去抓 robots.txt）的 SSL 驗證
   失敗，即使 `requests`／`curl` 都能正常連得上。程式原本假設「robots.txt
   讀取失敗就當作允許」，但失敗時被存進快取的是那個「讀取失敗、完全空」
   的 `RobotFileParser` 物件——這個物件的 `can_fetch()` 在從未成功讀取過
   （`last_checked` 是 0）時預設回傳 **False（不允許）**，跟程式原本註解
   假設的「空 parser 預設允許」正好相反。結果是：同一個 host 第一次呼叫
   因為有 except 接住還算正常（回傳 True），但這個「讀取失敗」的 parser
   物件被存進快取後，之後每一次呼叫都改成直接呼叫這個壞掉的 parser，
   每次都回傳 False——等於「第一次允許，之後全部變不允許」。改用一個
   獨立的 `_ROBOTS_UNREADABLE` sentinel 明確記錄「這個 host 讀不到
   robots.txt」，之後一律回傳 True，不再呼叫壞掉的 parser；同時加了
   `threading.Lock`，避免 `screen_stocks()` 用 `ThreadPoolExecutor` 平行
   處理多檔股票時，多個 thread 在快取還沒寫入前重複對同一個 host 發送
   robots.txt 請求。
2. **`providers/price._fetch_twse_official()` 把 dataclass 直接存進
   JSON 快取**：`cache.cached_call()` 存快取時用 `json.dumps()`，但這裡
   直接傳了 `list[PriceBar]`（dataclass 物件），一存就整個拋出
   `TypeError`。改成跟 `_fetch_finmind()` 一致的做法：`cached_call()`
   只快取原始 JSON-safe 的 `dict`，`PriceBar` 的組裝放在快取呼叫外面。

兩者都用「6 檔股票同時查詢」（模擬 `screen_stocks()` 實際的併發情境）驗證
過確實修好了；也用真實資料重跑過整個流程確認證交所備援本身功能正常。

## 14.2 尚未解決：FinMind 與證交所似乎都會封鎖 Render 這類雲端主機的 IP

**這是目前最大的未解問題，使用者還在考慮怎麼處理，之後接續開發前務必先
確認狀態有沒有變。**

修好 14.1 那兩個 bug 之後，50 檔（0050）的篩選結果變好看了，但使用者用
「上市市值前 150 大」（150 檔）實際測試時，回報又讀取很久，追查發現
**150 檔裡有 104 檔失敗**，全部失敗原因一致：`https://www.twse.com.tw/
exchangeReport/STOCK_DAY` 回傳 **428 Client Error**（伺服器主動拒絕，
不是逾時、不是我們程式的 bug）。合理推論：FinMind（402）跟證交所
（428，且看起來要達到一定請求量才會被觸發——50 檔那次能大部分成功，
150 檔請求量更大就被擋下來）**都對 Render 這類雲端/機房 IP 不友善**，
這不是程式碼能修的問題，是外部資料來源對雲端主機 IP 的封鎖/流量管制
行為。

跟使用者討論過的下一步（**尚未執行、使用者要求先讓他考慮**）：
1. **先試最低成本的方案**：去 FinMind 官網免費註冊帳號拿一組 token，
   填進 Render 的環境變數（`config.py` 已經支援 `FINMIND_TOKEN`，本地端
   目前沒有設定過這個變數）——如果 FinMind 對「有註冊 token」的請求跟
   「完全匿名」的請求給的待遇不同，有機會直接繞過 402 封鎖，讓 FinMind
   重新變回主要、快速的資料來源，證交所備援也就不需要再頂上。
2. **如果連 token 都沒用**：代表 Render（或同類雲端 PaaS）的 IP 大概率
   被兩邊都列入某種黑名單/流量管制名單，屆時要考慮換一種部署方式——
   例如用 Tailscale 之類的工具，讓 App 連回使用者家裡這台電腦的真實
   網路（住宅 ISP IP，這兩個資料來源目前看起來都沒有擋），取捨是這台
   電腦要保持開機、API 服務要保持在跑，才能在外面連得到。

**還沒有驗證過的一件事**：現有的 Streamlit Community Cloud 部署（網頁版）
會不會也遇到一樣的封鎖——如果 Streamlit Cloud 的 IP 範圍跟 Render 不同、
沒有被列入黑名單，那這個問題就是 Render 專屬的；如果連 Streamlit Cloud
也一樣被擋，代表問題範圍更大，牽涉到現有正式網頁版的可用性，不只是這次
新增的手機 App。之後排查這個問題時應該先確認這件事。

## 15. 如果要繼續開發，建議先看這幾個檔案

- `ma_screener.py` — 均線分級邏輯核心，加新的分級規則或新股票池大概率要碰這裡；
  也是技術分析圖表的 history 序列（K 棒/均線/KD）產生的地方
- `kd.py` — KD 指標計算，跟均線（`moving_average.py`）分開維護，之後要加其他
  技術指標可以比照這個檔案的模式獨立開一個新檔
- `etf0050_constituents.py` / `etf0051_constituents.py` — 股票池清單，每季要
  手動查核更新
- `providers/price.py` — 股價來源（FinMind + 證交所備援），跟公司基本面分析
  專案是分開維護的複本，修 bug 記得檢查另一邊；目前 FinMind／證交所在 Render
  上都會被擋（見第 14.2 節，尚未解決），改這個檔案前先確認那個問題的最新狀態
- `http_client.py` — robots.txt 檢查跟共用的 GET 邏輯，第 14.1 節修過一個
  「robots.txt 讀取失敗時判斷邏輯反了」的 bug，改這裡要小心 `_robots_cache`
  的 sentinel 設計，不要又存回一個「讀取失敗」的 parser 物件
- `screen_page.py` / `pdf_report.py` — 兩種輸出格式，改呈現內容通常要兩邊一起改；
  `screen_page.py` 現在同時扛著表格篩選 UI 跟 Canvas 技術分析圖表兩套前端邏輯，
  是這個專案目前 JS 最複雜的檔案，改動前建議先搞清楚 `drawPanel()` / `renderChart()`
  / `chartRange` 縮放模型，以及第 12 節的觸控手勢意圖判斷邏輯，再動手修改
- `app_streamlit.py` — 網頁介面，PC/iPhone 開啟方式的細節、側邊欄/popover 選單
  切換邏輯、iframe 高度自動調整都在這裡
- `api.py` — 給手機 App（見第 14 節、`../篩選器APP/`）用的 HTTP API，跟
  `app_streamlit.py` 平行的另一個前端入口，改篩選/報告邏輯本身兩邊都要留意
  是否需要同步驗證（不需要同步改程式碼，兩邊都是直接 import 同一套底層函式）
